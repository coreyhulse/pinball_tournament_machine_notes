
#### Custom ROM

- There is a tournament ROM that with tournament mode on will used a fixed value skill plunge and outlanes.

When using the tournament ROM, set the following settings:

- Tournament mode on
- Catch Up off
- Novelty mode on
  - normal extra balls and specials are worth points
